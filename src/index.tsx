import React from 'react';
import ReactDOM from 'react-dom/client';
import 'src/algorithms/search/binary';

const root = ReactDOM.createRoot(
    document.getElementById('root') as HTMLElement
);
root.render(
    <React.StrictMode>
        <div>Алгоритмы сила</div>
    </React.StrictMode>
);
